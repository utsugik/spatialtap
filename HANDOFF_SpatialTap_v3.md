# HANDOFF — Spatial Tap 開発 (v3)

最終更新: 2026-07-10
作成: Koichi Utsugi (KU)
このドキュメントは Claude.ai プロジェクト「Spatial Tap 開発」の単一の情報源（single source of truth）です。
新しいチャットを始めるときは、まずこれを読んでから作業を開始してください。
（v2→v3 の主な変更: Fable 5 監査・Sonnet 5 査読・レビューの結論を「14章 決定事項」として確定。次フェーズは実装。）
（v3内更新 2026-07-10: GitHub Pages公開完了・iPhone実機検証成功を反映。次は8章-2「音の説得力3点セット」。）
（v3内更新 2026-07-10: 2章「設計思想」を新設。）
（v3内更新 2026-07-10: 3章「空間モデルの位置づけ」を新設。）

---

## 0. このチャットで最初にやること

- このHANDOFFと、知識ベースの index.html / spatial-tap_v1_room.html を読む。
- 「今どの段階か」は本書 8章、確定した作業は 14章「決定事項」を参照。
- 大きな決定や新しい完成版が出たら、**必ずこのHANDOFFを更新して知識ベースを差し替える**（最重要の運用習慣）。
- **現在の段階: 相談フェーズは完了。次は実装フェーズ（Claude Code へ移行）。**

---

## 1. コンセプト（最重要・ぶれさせない）

**一般ユーザーが、特別な機材も知識もなく、iPhoneで撮った普通の動画だけで立体音響を気軽に体験できるようにし、立体音響そのものを広める。**

- 360°カメラや専用機材は「不要」であることが価値の核。普通のiPhone動画で成立させる。
- 難しい設定を強いる解決策は最終形では不可。誰でもそのまま使えることを最優先。
- 主対象デバイス: iPhone（Chrome / Safari 本体。※iOSのChromeも中身はWebKit＝Safariと同一エンジン）。Android はスコープ外。
- **核の操作は「タップ＝音がそこへ行く」の1動作。** 高度な機能はこれを邪魔しない範囲で。

---

## 2. 設計思想 — 誇張された体験としての立体音響（2026-07-10 決定）

本アプリは物理的な正確さの再現を目的としない。目的は「立体音響の面白さ・凄さ・可能性を体感させ、興味を持ってもらう」こと。

- アニメーション、トリックアート、漫画と同じく、正確な再現より「効く誇張・面白い錯覚」を優先する。人は日常で自然な立体音響の中に生きているため、正確な再現は「日常と同じ」としか感じられず、驚きは生まれない。驚きは「日常ではありえない音の動き」からのみ生まれる。
- 立体音響が一般に広がらないのは、正確さを競いエンタメ性を軽視してきたためという仮説に基づく。本アプリはその空白を埋める。
- ただし「誇張」は「破綻していい」という意味ではない。クリッピングや不快な酔いを起こさず"崩さずに大げさにする"のが技術。音質の土台（歪まない・破綻しない）は誇張とは別に必須。
- 今後の全判断（機能の優先順位・誇張の量・見せ方）は、この思想を基準にする。「物理的に正しくするには」ではなく「面白く・驚かせるには」で考える。
- 誇張の量に正解はなく、理論からは導けない。今後の調整は実機で聴いて感覚で決める領域に入る。

---

## 3. 空間モデルの位置づけ（2026-07-10 確認）

本アプリの立体音響方式を、混同を避けるため明記する。

- 方式は「バイノーラル」（2ch・ヘッドホン/イヤホン前提でHRTFにより頭内に立体を作る）。マルチチャンネルスピーカー方式とは目的も仕組みも異なる。スピーカー再生では効果がほぼ消えるため、ヘッドホン必須はデザイン上の親切ではなく方式上の必須要件。
- 空間の考え方は「座標空間モデル」（PannerNodeで3D座標の任意の点に音源を置く）。聴き手を取り囲む球面に音を貼るAmbisonics / Sony 360 Reality Audioのような球体モデルではない。「広大に広がる空間に音源を配置する」という本アプリのイメージは座標空間モデルと一致しており、球体モデルへの移行は不要。
- 思想的にはDSpatial（座標空間+物理モデル系、KUが普段使用。特にバイノーラル製品のReality VRを使用）と同系統だが、精度はWeb Audio標準機能の範囲で全く及ばない。ゆえに精度で並ぶことは目指さず、2章の設計思想どおり「誇張された面白い体験」で勝負する。
- 球体モデル（Ambisonics）は、将来「全方位録音の360度動画」を扱う版を作る場合にのみ再検討する（将来課題）。

---

## 4. 現在の成果物（ファイル）

知識ベース＆ローカル `~/Documents/Claude/Spatialtap` の両方に格納:
- **index.html** … 現行の本命。動画版（方式A）。GitHub Pages でそのまま使える名前。
- **spatial-tap_v1_room.html** … 復元ポイント（凍結スナップショット）。いつでもここに戻れる。触らない。
- **HANDOFF_SpatialTap_v?.md** … 本書。最新版のみ知識ベースに残す（古い版は削除）。

作業の起点は index.html。壊したら v1_room に戻す。

---

## 5. 実装済みの機能（index.html）

- 動画読み込み→画面タップ/ドラッグ地点に音源定位（Web Audio PannerNode + HRTF）。タップ地点は保持、次タップで切替。
- 画面 横=方位(±90°) / 縦=高さ(±45°)、距離は Distance フェーダー。
- モノ/ステレオ自動判定（decode成功時 buffer.numberOfChannels）。モノ=1点、ステレオ=L/R 2点。
- Width（L/R間隔）、Room（合成IRリバーブの長さ・量）、yaw/pitch/roll（L/Rペアを3D回転・無限回転・ダブルタップで0）。
- 右上「LOG」ボタン: iPhoneでも状態/エラーを画面表示（デバッグ用）。

---

## 6. 音響設計の要点（なぜこの実装か）

- ステレオは ChannelSplitter で L/R 分離→別々の PannerNode。左右が別の点として定位し幅・回転を正しく扱える。
- 3D座標は center() + rotOffset() の1か所で計算。視覚・音響・数値が必ず一致。
- Web Audio 座標系: 前方=-z、右=+x、上=+y。listener は forward(0,0,-1)/up(0,1,0)。
- iOS対策: 動画はミュート再生（映像専用）、音声は decodeAudioData で空間化。createMediaElementSource の iOS 無音バグを回避。デコード失敗時のみ element 音声にフォールバック。

---

## 7. 判明済みの問題と原因

### (A) Claudeアプリ内プレビュー（WebView）では動画が再生できない ← 最大の障壁
- H.264 の .mov でも `VIDEO ERROR code=4` が出る。原因はコーデックでなくWebViewの再生制限。
- 対策: 実ブラウザ（iPhone Chrome/Safari 本体）で開く=HTTPS URL必須=ホスティング必須。
- ※未検証: 実ブラウザ本体での .mov(H.264/HEVC) 再生可否は GitHub Pages 公開後に確定。

### (B) コーデック
- 画面収録/高効率設定は HEVC/.mov。「設定→カメラ→フォーマット→互換性優先」でH.264（切り分け用。最終形では強制しない）。
- iOS ChromeもWebKitのため「Chrome/Safari本体」の検証は実質1エンジンで済む。HEVCはApple自身の形式なので実ブラウザでは再生できる見込みが高い（※要検証・仮説）。

### (C) ノブが動かない（解決済み）
- Pointer Events + setPointerCapture、子要素 pointer-events:none、pointerdown で stopPropagation。実機確認済み。

### (D) 動画が「no video」のまま（原因確定）
- 選択は伝わっている（LOG確認済み）。表示されないのは (A) のWebView制約が主因。

---

## 8. 次にやること（優先順・14章の決定を反映）

1. ✅ **完了（2026-07-10）: GitHub Pages 公開 → iPhone 実ブラウザで動作確認。**
   - 公開URL: https://utsugik.github.io/spatialtap/ （Publicリポジトリ: https://github.com/utsugik/spatialtap ）
   - 実機検証結果: `VIDEO ERROR` なし、`audio decoded` ch=2 dur=189.4 sec（.mp4/H.264）で成功。
   - **確定した結論: 検証したファイル（.mp4/H.264）は実ブラウザで問題なく再生・decode可能。ffmpeg.wasmは現時点で不要（14章「非採用」のまま据え置き）。**
   - ※注記: 今回検証したのは .mp4/H.264。HEVC/.mov単体での再生可否はまだ未検証のまま（7章(B)参照、今後実際にHEVC素材を使う場面が来たら確認）。
2. ✅ **完了（2026-07-10）: 音の説得力3点セット**（index.html 実装済み・未pushの実機再検証はこれから）。
   - (a) 距離連動の dry/wet 比: panner→convの直結を独立GainNode `send` 経由に変更。距離0.4〜12mを send.gain 0.15〜1.0にマッピングし、遠いほどwetが増える構成にした。
   - (b) 出力リミッター: dry/wetの合流前に `DynamicsCompressor`（threshold -3dB, ratio 20, attack 3ms, release 150ms）を追加。
   - (c) Room フェーダーのデバウンス: `fRoom` の input イベントで `makeIR()` を直接呼ばず、150ms の `setTimeout` 経由に変更（ドラッグ中の多重生成を防止）。
   - ※距離連動ローパス・IR初期反射（14章「採用」の別項目）は今回のスコープ外。次の作業候補として残っている。
   - **不具合と修正（2026-07-10）: 実機検証で、距離に関わらず常時「パリパリ」というクラックリングノイズが発生。原因はオーディオグラフの二重接続ではなく（確認済み・問題なし）、リミッターがヘッドルーム不足で追いつかずAudioDestinationNode手前で瞬間クリップしていたためと判断（電子ドラム等の速い立ち上がりで顕著）。対策として dry ゲインを1→0.6（-4dB相当）に下げてヘッドルームを確保し、リミッターも threshold -3→-9dB・knee 0→6・ratio 20→8・attack 3ms→5ms・release 150ms→200msに変更し、急な潰し方をやめて早め・穏やかに効かせる設定にした。**
   - **クラックリング再検証結果（2026-07-10）: 別の動画では発生しないことを確認。実装バグではなく、元動画の音声に含まれるノイズ成分が近距離時のゲインで増幅されて聞こえていた可能性が高いと判断。実装側の追加対応は行わず、一旦保留（様子見）とする。** 今後同様の症状が別素材で再現した場合のみ再調査する。
3. ✅ **完了（2026-07-10）: Distance範囲拡張（0.4〜12m → 0.4〜100m）**。
   - PannerNode の maxDistance を 80→100 に拡張。refDistance/rolloffFactor（近距離側の減衰特性）は変更なし。
   - 残響送り量（send.gain）のマッピングを新レンジに合わせ対数スケールで再設計（0.15〜1.0）。
   - 遠距離時に完全無音化しないよう、PannerNode自体の距離減衰を補償する `distComp` ゲインノードを追加。10m以下（floor=-20dB到達前）は補償なし＝近距離側の挙動は変更なし。10m超で緩やかにブーストし、100mでも最低-20dB相当の音量を確保。
   - Distanceスライダーの操作感を対数カーブに変更（内部は0〜1000のポジション値を指数変換して実際の距離に反映。近距離側ほど細かく調整できる）。
   - ※`distComp`のロジックはこの後 5. で全面刷新（「floor到達前は無補正」→「奥行き軸から一貫した音量カーブを生成」に変更）。
4. ✅ **完了（2026-07-10）: フライバイ移動**（2章「設計思想」に基づく最初の誇張演出。index.html 実装済み・実機再検証はこれから）。
   - タップ/ドラッグの位置（ctrl.u/v）は「目標地点」として即座に更新し続ける。実際に音・ドットが表示される位置（dispU/dispV）は、目標へ向けて毎フレーム指数関数的に追従する別状態にした（時定数0.11秒、約0.3〜0.4秒でほぼ収束）。
   - 目標までの残り距離に応じて表示位置を上方向へ弧状に持ち上げ（最大0.07、収束とともに自然に0へ戻る）、視覚・音響の両方に同じ`flyingPos()`を使うことで映像と音を一致させた（center()/updateDots()共通利用）。
   - ドップラー（ピッチ変化）は入れず、飛行中だけ`flyGain`ノードで音量を最大15%ディップさせ、収束とともに1.0へ戻す構成にした（音楽・演奏素材での音程破綻を回避）。
   - CSSの`.dot`にあった`transition:left/top`は、JS側の毎フレーム補間と二重に効いてもたつくため削除。
   - ドラッグ中に目標が連続で変わる場合も、常に最新の`ctrl.u/v`へ追従を続けるだけの設計のため、アニメーションの多重管理・破綻は生じない。
5. ✅ **完了（2026-07-10）: 奥行き軸への統合（距離表現の一本化）**（2章の設計思想に基づく誇張強化。index.html 実装済み・実機再検証はこれから）。
   - ※実装前確認: 依頼文にあった「今日追加したローパスフィルタ」はコード上に存在しなかった（14章「採用」に挙がっていたのみで未着手）。本項目で新規追加した。
   - 距離(0.4〜100m)を正規化した奥行き値 `depth`（0=最近〜1=最遠）を`depthOf(dist)`として新設。対数距離比率を前寄せカーブ `1-(1-raw)^N`で誇張し、近距離側の変化を大きく感じさせる。
   - 以下を`depth`から同一カーブで駆動する構造に統合: 音量（`distComp`を「floor到達前は無補正」から「奥行きカーブに基づく音量へ全面置換」、境界値(0.4m→変化なし／100m→-20dBフロア)は既存の較正から変更なし）／高域減衰（新規`BiquadFilterNode`、20000Hz→1200Hzへ対数的にスイープ）／残響送り量（`send.gain`のマッピング元を「距離の対数比率」から`depth`に変更、範囲0.15〜1.0は既存のまま）／ステレオ幅（新規。`rotOffset()`に`widthMul`引数を追加し、音の合成にのみ`depth`に応じた幅縮小(最遠で5%まで)を適用）。
   - フライバイ移動との干渉なし: `depth`は`ctrl.dist`（Distanceフェーダー値）のみに依存し、タップ位置(`dispU/dispV`)には依存しないため、フライバイ中に幅やフィルタが変動することはない。
   - 全パラメータとも`setTargetAtTime`で滑らかに追従させ、スライダー操作・フライバイ中のプチノイズを防止。
   - **誇張指数の引き上げ（2026-07-10）**: 実機フィードバック「変化が控えめで驚きとして弱い」を受け、`DEPTH_EXAG_N`を1.5→3へ引き上げ。N=3では近距離2割相当(raw=0.2≈1.2m)でdepthC≈0.49に達し、その時点で音量-9.8dB・幅53%・LPF約5kHzまで変化する（node上で検算済み・実機聴感は未検証）。デフォルト距離3mでは-14.9dB・幅29%・LPF約2.5kHzとなり、明瞭さは保ちつつ明確に奥まった印象になる想定。100mでの境界値（-20dBフロア）は変更なし。
6. ✅ **完了（2026-07-10）: WIDTHフェーダー廃止**（誇張路線に合わせパラメータを絞る。「奥行きで自動収縮」と「手動上限」の併存が分かりにくいとのフィードバックを受けて削除）。
   - UI（フェーダー本体・ラベル・数値表示）を削除。
   - 内部的には`ctrl.width`を廃止し、常にWidth=100%を上限として`depth`による自動収縮のみが効く形に固定（`rotOffset()`の`widthMul`引数はそのまま活用）。
   - **見た目は意図的に変更していない**: `updateDots()`は従来どおり`rotOffset()`を引数なし（=幅縮小なし・常に最大幅）で呼ぶため、画面上のL/Rドット間隔は固定。音だけが遠くなるほど幅が狭まる。
   - Distance/Roomフェーダー・他の既存機能への影響なし（`ctrl.width`はrotOffset()以外から参照されていなかったため、削除は局所的）。
7. ✅ **完了（2026-07-10）: ドットの三段変化 ＋ 数値readoutのデフォルト非表示**（2章の設計思想。index.html 実装済み・実機再検証はこれから）。
   - ドット（L/R/モノ）の見た目を`depthOf(ctrl.dist)`と連動させ、遠いほど「小さく・ぼやけて・暗く」変化させた（初期実装。下記の不具合修正で数値を改訂）。
   - 音（音量・高域減衰・残響・幅）とドットの見た目は同じ`depthOf(ctrl.dist)`を参照しているため、常に一致する。フライバイ移動中も`updateDots()`が毎フレーム再計算するため、位置と同時に見た目が滑らかに追従する（距離自体はフライバイでは変化しないため、見た目の奥行き変化はDistanceフェーダー操作時のみ発生）。
   - **不具合と修正（2026-07-10）**: 実機確認で「5m程度でドットがほぼ見えなくなる／最近距離でも的として小さい」との指摘。原因は、音響用`depthOf()`がN=3で前寄せが強く、5mの時点で既にdepthC≈0.84（ほぼ底値）に達しており、視覚カーブがこれをそのまま使っていたため。対策として、**音響側の`depthOf()`・`DEPTH_EXAG_N`は変更せず**、視覚専用に`depthVis = depthOf(dist)^DOT_VIS_POW`（DOT_VIS_POW=4）という緩和カーブを追加。5m→depthVis≈0.50、10m→depthVis≈0.74、30〜100m→0.96〜1.00と、近〜中距離の劣化を穏やかにし、極端に遠いときのみ顕著にした。あわせて下限を引き上げ（最小サイズ0.4→0.65、最小明度0.4→0.65、最大ぼかし5px→3px）て完全に見えなくならないようにし、基準サイズ自体も拡大（M 22→28px、L/R 26→32px）してタップの的として大きくした（node上で新カーブを検算済み・実機聴感は未検証）。
   - 数値readout（azimuth/elevation/distance）は通常表示から非表示に変更。既存のLOGボタンと連動させ、LOGを開いたときだけ`.readout.on`で表示する方式にした（CSSに`opacity:0→1`のトグルを追加、新規UI要素は増やしていない）。
   - ステータス表示（IDLE/PLAYING等）・ヘッダー要素は変更なし。
8. ✅ **完了（2026-07-10）: ピンチでDistance(奥行き)を操作**（1章「タップ1動作」・2章の設計思想。index.html 実装済み・実機再検証はこれから。Distanceフェーダーは削除せず併存、使用感を見て今後の削除判断）。
   - ステージ上の2本指ピンチジェスチャーを検出し、`ctrl.dist`を直接操作する。つまむ(指が近づく)=近づく、広げる(指が離れる)=遠ざかる。
   - 感度`PINCH_SENS=3`（指の開閉1pxあたりDistanceポジション3単位/1000）。指の開閉幅 約330pxでほぼ全域(0.4〜100m)を走破できる値を初期値とした（3m付近で30px→約1.8〜4.9m、100px→約0.6〜16mと変化する計算に基づく。実機の操作感で調整前提）。
   - ピンチ操作の結果はDistanceフェーダーのつまみ・数値表示にもリアルタイムに反映し（`$('fDist').value`を直接更新）、フェーダー側から見ても操作結果が分かるようにした。フェーダーを手で動かす従来の操作も変更なし（同じ`ctrl.dist`を共有）。
   - ブラウザ標準のピンチズームとの競合: `#stage`は既存の`touch-action:none`によりブラウザ標準のピンチズーム/パンは元から無効。念のためiOS Safari系の`gesturestart/change/end`イベントも`preventDefault`する保険を追加した。viewportメタタグの`user-scalable=no`と合わせ、二重・三重に対策している。
   - **操作ルールの統一（2026-07-10）**: 初期実装は「2本指＝距離のみ、位置決めは中断」という段階分けだったが、「1本指＝位置のみ、2本指＝位置(重心移動)＋距離(間隔変化)を同時に」という単純なルールに書き換えた。触れている指の重心が常にXY位置(ctrl.u/v)を駆動し、2本指のときだけ追加で指の間隔がDistanceも同時に駆動する（`trackedCentroid()`/`applyTrackedPosition()`で位置、既存のPINCH_SENSロジックを流用して距離を並行更新）。
   - **ジャンプ防止**: 指の本数が変わる瞬間（1→2、2→1）に、現在の`ctrl.u/v`を保ったまま新しい基準点との差分をオフセット(`posOffU/posOffV`)として再計算することで、位置が飛ばないようにした。何も触れていない状態から新しく触れた瞬間（真新しいタップ）だけはオフセットを0にし、従来通り「タップ＝その場に瞬間移動」の絶対位置マッピングを維持する。3本目以降の指は位置・距離どちらの操作にも使わない（無視）。
   - 不要になった`dragging`フラグと旧`fromPointer()`は削除し、位置決めロジックを`applyTrackedPosition()`に一本化した。
9. ✅ **完了（2026-07-10）: Roomフェーダー廃止**（距離連動の残響送り量(send.gain)だけで距離の説得力は十分と判断し削除）。
   - UI（フェーダー本体・ラベル・数値表示）を削除。
   - `ctrl.room`を廃止し、`makeIR()`のIR長を固定値(1.0秒)に変更。`wet.gain`もRoom依存の計算をやめ、生成時の固定値(0.18)のまま据え置いた。Room値変更時に多重生成を防いでいたデバウンス(`roomDebTimer`)も、Roomフェーダー自体が無くなったため不要になり削除。
   - **Distance最小=ほぼDry、Distance最大=十分な残響、という一貫した挙動**は既存のdepth駆動`send.gain`（0.15〜1.0、7章-5参照）がそのまま担っており、Room削除後もこの挙動に変化はない。
   - Distanceフェーダー・他の既存機能への影響なし。
10. ✅ **完了（2026-07-10）: Distance数値表示を「m」→「%」に変更**（2章の設計思想。誇張された演出値であり物理的に正確な距離ではないため、単位付き数値による誤解を避ける）。
    - `distPercentText(dist)`を新設し、`posFromDist(dist)/1000*100`で0〜100%に正規化して表示。0.4m→0%、100m→100%（境界値をnode上で検算済み）。
    - **内部の距離計算・音響/視覚への影響（depthOf・音量・高域減衰・残響・幅・ドットの見た目）は一切変更していない。表示（Distanceフェーダー横の数値テキスト）のみの変更。**
    - フェーダーのつまみ位置・ピンチとの連動ロジックも変更なし。フェーダー操作・ピンチ操作・初期表示の3箇所すべてで同じ`distPercentText()`を使い一貫させた。
11. ✅ **完了（2026-07-10）: オービット（背後周回演出）**（2章の設計思想「日常ではありえない音の動き」の目玉演出。index.html 実装済み・実機再検証はこれから）。
    - 起動: 音源ドットのダブルタップ（タップ座標がドットから50px以内、350ms以内の2回タップで判定）。
    - 動き: ダブルタップ時点の方位/仰角を起点(az0)に、方位を`az0→az0+360°`まで2.5秒(`ORBIT_DUR`)かけて一周させ、元の位置に戻す。通常のu/v(前方半球±90°のみ有効)とは別に、方位を直接±180°まで扱う専用ロジックとして実装した（既存の`center()`は方位のクランプをしていないが、依拠が偶発的になるのを避けるため独立関数`orbitStep()`にした）。
    - 距離は`ctrl.dist`から独立した固定値`ORBIT_DIST=1.5`m相当を使用。周回中は`depthOf(ORBIT_DIST)`が一定のため、音量(-11.2dB)・高域減衰(LPF約4.1kHz)・残響送り量(63%)・幅(47%)が周回中ずっと一定に保たれる（近すぎず遠すぎず明瞭さを優先する値をnode上で検算して選定）。
    - 視覚: 方位が前方半球(|az|≤90°)の間は通常のドット表示（三段変化の見た目カーブも維持）、背後(|az|>90°)の間はドットを非表示にし、背後寄りの方位に近い画面端（左右いずれか）が発光する`.edgeglow`要素を新設。発光強度は90°地点で0→180°(真後ろ)で最大になるようにした。
    - **オービット中の割り込みは「許可しない」を選択**: `stage`の`pointerdown`/`pointermove`に`if(orbit) return`を追加し、タップ/ピンチ操作を無効化。`refresh()`自体にも`if(orbit) return`のガードを追加し、Distanceフェーダー等の外部入力によるちらつき（通常描画とオービット専用描画の競合）も防いだ。
    - 終了後は`ctrl.u/v`を一切変更していないため、ダブルタップした元のタップ位置へ自動的に戻り、通常のタップ/ピンチ操作にそのまま復帰する。
12. **モノ音声フォールバック時の R チャンネル無音バグ修正**（小さいが「壊れてる」誤解を招くため優先）。
13. （保留・体験拡張）デモ動画同梱、ジャイロ連動、軌跡の記録/再生。
14. （保留・要実機調整）高さ方向のEQ強化、方位マッピングの実測合わせ（±35°/±90°の切替）。

※ ffmpeg.wasm 変換は 1 の結果次第で不要になる可能性が高い。着手は保留。

---

## 9. ツールの使い分け

| 作業 | ツール |
|---|---|
| コンセプト相談・監査・理論（Fable 5 / Sonnet 5） | claude.ai プロジェクト内チャット |
| コード実装・修正・デバッグ、git操作・公開・更新 | **Claude Code**（git push で高速。次フェーズはこちら中心） |

- **相談フェーズは完了。ここからは実装・検証フェーズ = Claude Code が主戦場。**
- 相談で重要な結論が出たら、実ファイル書き換え/実機検証は Claude Code、決定の記録は本HANDOFF。

---

## 10. 既知の限界・確認が必要な点（正直な注記）

- 高さ(上下)の聴感は汎用HRTFのため弱め（個人差）。強化は PHASE/実測HRTF が必要。
- 平面動画のため真後ろは非対応（前方半球）。360°は別アプローチ（球体版/Three.js）。
- decode方式は映像と音声が別系統→長尺ループで微小ズレの可能性（短尺は問題なし）。
- リバーブは合成IR（減衰ノイズ）で初期反射なし＝「広さ」でなく「残響長」だけが変わる状態（14章で改善対象）。
- **普通の動画の音声は環境音・声などが混ざった1本のミックス。「動画内の特定の物だけを分離」はできず「音声全体を配置」する体験。** デモ/説明で期待値を正しく設定する必要あり。
- スピーカー再生では効果がほぼ消える → **UI最初にヘッドホン必須を明示**（離脱の最大リスク回避）。
- 数値提案（例: 8kHz±3〜4dB、FOV±30〜35°）は方向性は妥当だが未検証の経験則。実機で聴きながら調整前提。
- HEVC実機再生可否・decodeAudioData可否・ffmpeg.wasm要否・その実機性能はいずれも未検証。

---

## 11. 環境メモ

- 公開先: GitHub Pages（Public・URL固定）。Netlify Drop は使い捨て確認用（日次制限・URL可変）。
- ローカル作業フォルダ: `~/Documents/Claude/Spatialtap`（他のKUプロジェクトと同じ ~/Documents/Claude 配下）。
- クラウドのプロジェクト知識とローカルフォルダは別物。両方に最新を置く。
- ⚠ プロジェクト内でも**会話履歴は次の新チャットに自動で引き継がれない**。引き継がれるのは知識ベースとカスタム指示のみ。だから重要な決定は必ず本HANDOFFに追記して差し替える。

---

## 12. デザイン言語（KU）

- KU共通の基本形: 暗い背景、cyan/amber系アクセント、等幅フォント（Space Mono / JetBrains Mono）。
- **Spatial Tap は、この共通デザイン言語から意図的に切り離した独自の世界観を採用する**。
  他のKUプラグイン・Webの見た目を変えるものではなく、本アプリ限定の変更。

### 現行版「Lavender / Nunito」（2026-07-10 全面刷新）

社内モックアップ「Spatial Tap - 2a Lavender.html」の配色・フォント・質感を正確に適用したもの。以前の「Zen Maru Gothic・黒背景・L=アクア/R=コーラル」版（同日の前段の変更、下記に履歴として残す）をさらに置き換えている。理由: 「エンジニアっぽい計測器的な見た目」から「夜に浮かぶ親しみやすい体験」へ、より柔らかい北欧的な方向へ寄せるため（HANDOFF 2章の設計思想に基づく）。CSSのみの変更で、HTML構造・JavaScript（タップ配置・ピンチ・フライバイ・オービット・奥行き軸駆動の音響処理など全機能）は一切変更していない（`<script>`タグ内の差分ゼロをgit diffで確認済み）。

- フォント: **Nunito**（Google Fonts, weight 400/500/600/700/800, font-display:swap）に統一。見出し（Fraunces italic）も含め、操作系・見出しとも全面的にNunitoへ置き換えた（前回の「見出しだけFraunces維持」から方針変更。モックアップ自体が全要素Nunitoのため、正確な適用を優先）。Nunitoは英数字専用フォントのため、日本語文字（聴空間・タップ操作の説明文等）はブラウザ/OS標準の和文フォントに自動フォールバックする（モックアップ自体もこの前提で作られている）。
- 背景: 単色の暗黒(#06070b)から、紫〜濃紺のグラデーション `linear-gradient(180deg,#574f74 0%,#433a5e 55%,#322c47 100%)` に変更。
- 色（L/Rの色相区別は機能上の意味があるため維持）:
  - L(左) = ミント系 `#9fe3cf`
  - R(右) = ピンク系 `#f6b6d1`
  - listener/UI/差し色 = ピンク系アクセント `#eba9c8`
  - モノ(アンバー) `#ffb13d` は維持
  - 枠線・区切り線は、背景が明るくなったことに伴い黒系(`--line`/`--line2`)から半透明の白系(`rgba(255,255,255,.09)`/`rgba(255,255,255,.14)`)に変更。
- 質感: ボタン・バッジ・動画パネルの角丸を大幅に拡大（例: ボタン16px、動画パネル24px、バッジ20px）。動画パネルは薄い白グラデーション＋薄い白枠のカード状に。ノブはモックアップに合わせて紫系の暗色radial-gradientに変更し、外周にピンク系conic-gradientの装飾リング（`::before`疑似要素、HTML構造は変更していない）を追加。スライダーのつまみもノブと同系の暗色radial-gradient＋ピンクの縁取りに変更。
- ドット・気配グロー（オービット、11章参照）の色も、この新しいアクセントカラーに追随済み。ドット内のL/R文字色も、モックアップに合わせて背景色と調和する濃色（L=`#1f3b34`、R=`#5a2740`）に変更。
- 動画エリア: `#stage`のグローをラベンダー系(`rgba(196,181,253,...)`)からピンク系(`rgba(235,169,200,...)`)中心に調整。box-shadowが`overflow:hidden`と同一要素だと自身の影も切り取られてしまう問題は前回同様`filter:drop-shadow()`で回避している。
- **Distanceスライダーの塗りつぶしバー（2026-07-10 追加対応）**: モックアップの「track__fill」を再現。ネイティブ`<input type=range>`は値に応じた塗りつぶしを持たないため、`updateDistFill()`を新設し、`fDist`の`background`を`linear-gradient(to right, var(--you) 0%, var(--you) {pct}%, var(--line2) {pct}%, var(--line2) 100%)`へ都度書き換える方式にした。呼び出し箇所は初期表示・フェーダーのinputイベント・ピンチによる距離変更の3箇所で、いずれも距離計算・音響ロジックには一切関与しない見た目専用の追加。色は`--you`（`#eba9c8`）を使用。
- **YAW/PITCH/ROLLノブのダブルタップ/ダブルクリックで0リセット（2026-07-10 修正）**: 以前から`dblclick`イベントでの0リセット処理は存在したが、ノブの`pointerdown`で`e.preventDefault()`しているため、iOS等ではclick/dblclickの合成イベントが発火せず機能していなかった可能性が高いと判断（Pointer Events仕様上、pointerdownでのpreventDefault()は後続のクリック系イベント合成を抑制する）。`dblclick`リスナーを廃止し、`pointerdown`のタイムスタンプ間隔（350ms以内）で自前判定する方式に置き換えた。マウス・タッチいずれでも同じロジックで動作する。

### 旧版「Zen Maru Gothic」（2026-07-10、上記に置き換え済み・履歴として保持）
- フォント: JetBrains Mono → Zen Maru Gothic。見出しはFraunces italicのまま維持。
- 色: L=アクア系`#5eead4`、R=コーラル系`#fb7185`、listener/UI=ラベンダー系`#c4b5fd`。背景は黒(#06070b)のまま。

---

## 13. プロジェクト設定（現在値）

**説明「何を達成しようとしていますか？」:**
> 一般ユーザーが、特別な機材も知識もなく、iPhoneで撮った普通の動画だけで立体音響を気軽に体験できるWebアプリを作る。動画を再生しながら画面をタップした地点に音が定位し、タップし直すと切り替わる。ステレオはL/R2点として扱い、幅・距離・空間サイズ・yaw/pitch/roll回転を操作できる。立体音響を広めることが最終的な狙い。iPhone（Chrome/Safari本体）を主対象とし、GitHub Pagesで公開する。

**カスタム指示:**
> - 対象はiPhoneのChrome/Safari本体。Claudeアプリ内プレビューは動画再生が不可なので動作確認はGitHub Pagesの実URLで行う。
> - Web Audio + HRTF、動画はミュート再生で映像専用、音声はdecodeAudioDataで空間化する方針。
> - コード修正時はLOG機能を活かし、実機で検証してから完成と判断する。
> - 私は立体音響・Web開発の初学者なので、どのファイルをどこに置き何を押すか、手順を具体的に。

（監査結果: 説明文・カスタム指示ともコンセプトと一致、矛盾なし。運用設計＝単一情報源/復元ポイント/ツール使い分けは初学者向けとして妥当と評価された。）

---

## 14. 決定事項（2026-07-10 の監査・査読の結論）

Fable 5 が監査 → Sonnet 5 が査読（妥当な指摘と未検証の経験則を仕分け）→ 現行モデルがレビュー。結論を確定として記録する。

### 採用（実装する。優先度は 8章のとおり）
- **距離の dry/wet 比バグ修正**（最重要）。現状は距離を変えても直接音/残響比が一定＝実質音量フェーダー。convへの送りを独立 GainNode にし、距離に応じて wet を増やす。※行数は少なくても「信号経路の設計変更」である点に注意。
- **出力リミッター**（DynamicsCompressor）。distance最小時のゲイン増で歪む危険への対策。第一印象の「壊れてる」を防ぐ。
- **Room フェーダーの makeIR デバウンス**（約150ms）。ドラッグ中の多重生成でプチノイズ/処理落ちを防ぐ。
- **モノ音声フォールバック時の R 無音バグ修正**。フォールバック時はモノ扱いをデフォルトに。
- **距離連動ローパス**（遠いほど高域減衰）＋**IR に初期反射を加える/後半の高域を削る**（距離・空間サイズの説得力向上）。
- **UIにヘッドホン必須を最初に明示**。離脱防止。
- **メモリ対策**: ファイルサイズ上限チェック＆警告、URL.createObjectURL の revoke。iOSのタブクラッシュ防止。
- **初回再生の音量配慮**（控えめ〜フェードイン）※現行モデル追加提案。イヤホン直の不快な第一印象を防ぐ。

### 保留（土台が実機で動いてから／体験拡張）
- デモ動画同梱＋Orbit（自動旋回）ボタン。※ページを重くしない軽量デモにする。
- ジャイロ連動（DeviceOrientationEvent.requestPermission。許可前に理由を一言添える）。
- 軌跡の記録・再生。空間化動画の書き出し（MediaRecorder、iOS実用性は要検証）。
- 高さ方向のEQ強化（8kHz付近ブースト等）と方位マッピングの±35°/±90°切替。※数値は実機で聴いて調整。

### 非採用/現時点で着手しない
- ffmpeg.wasm によるブラウザ内変換（30MB級DLで「気軽さ」と矛盾）。※8章-1の実機検証で要否が確定するまで着手しない。
- アンビソニックス（Omnitone等）への置換。回転は数学的に自然だが実装複雑度大。将来の360°版で検討（3章「空間モデルの位置づけ」参照）。
- index.html と v1_room の重複一本化。※統合(13-保留)を実施するときのみ。今は触らない。

### 運用の追加ルール
- HANDOFF の各決定に日付を付す（後で「なぜこうしたか」を辿れるように）。
